global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'plama-server'
    metrics_path: '/metrics/prometheus'
    static_configs:
      - targets: ['host.docker.internal:3000']
        labels:
          app: 'plama'
          env: 'development'
